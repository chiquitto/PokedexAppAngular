CREATE TABLE pokemon (
  id INTEGER,
  identifier TEXT,
  species_id INTEGER,
  height INTEGER,
  weight INTEGER,
  base_experience INTEGER,
  order_id INTEGER,
  is_default INTEGER,
  PRIMARY KEY (id)
);